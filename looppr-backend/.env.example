# MongoDB connection string. Leave unset in local dev to auto-start an
# in-memory MongoDB instance (data is wiped on restart) — set a real
# MongoDB Atlas URI for anything persistent or production.
MONGO_URI=

# Port the API server listens on.
PORT=5000

# Origin of the frontend app, used for CORS + cookie settings.
CLIENT_URL=http://localhost:5173

# Secrets used to sign JWTs — use long random strings in production.
JWT_ACCESS_SECRET=replace-with-a-long-random-string
JWT_REFRESH_SECRET=replace-with-a-different-long-random-string

# Access tokens are short-lived; refresh tokens are long-lived and stored
# in an httpOnly cookie.
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=30d

# Signs the short-lived (10 min) token issued after a correct password check
# but before the OTP step completes. Must differ from the other JWT secrets.
JWT_LOGIN_CHALLENGE_SECRET=replace-with-a-third-long-random-string

# Business Portal JWT secrets — MUST differ from the customer/admin secrets
# above. This is what isolates the two auth systems: a business token signed
# with these can't be verified (or accepted) on customer/admin routes, and
# vice versa. If unset, the code falls back to JWT_ACCESS_SECRET/
# JWT_REFRESH_SECRET so local dev still runs — but set distinct values in
# production or the isolation guarantee is lost.
JWT_BUSINESS_ACCESS_SECRET=replace-with-a-fourth-long-random-string
JWT_BUSINESS_REFRESH_SECRET=replace-with-a-fifth-long-random-string

# Partner Portal JWT secrets — MUST differ from all the secrets above. Same
# isolation rationale as the business secrets. Falls back to JWT_ACCESS_SECRET/
# JWT_REFRESH_SECRET if unset (local dev), but set distinct values in prod.
JWT_PARTNER_ACCESS_SECRET=replace-with-a-sixth-long-random-string
JWT_PARTNER_REFRESH_SECRET=replace-with-a-seventh-long-random-string

# Driver Portal JWT secrets — MUST differ from all the secrets above. Same
# isolation rationale as the business/partner secrets. Falls back to
# JWT_ACCESS_SECRET/JWT_REFRESH_SECRET if unset (local dev), but set distinct
# values in prod.
JWT_DRIVER_ACCESS_SECRET=replace-with-an-eighth-long-random-string
JWT_DRIVER_REFRESH_SECRET=replace-with-a-ninth-long-random-string

# All OTP/notification emails (signup verification, 2FA login codes) go
# through services/emailService.js, which picks a provider based on this
# one line — switching providers never requires touching any other file.
#   gmail   dev default: Gmail SMTP, delivers to any recipient, no domain
#           verification needed. See GMAIL_* below.
#   resend  production: Resend, needs a verified domain (see
#           services/email/resendProvider.js) — RESEND_API_KEY/OTP_FROM_EMAIL
#           below. Resend's sandbox sender (onboarding@resend.dev) can only
#           deliver to the email tied to the Resend account, which is why
#           this defaults to gmail for local dev.
EMAIL_PROVIDER=gmail

# Used when EMAIL_PROVIDER=gmail. GMAIL_USER must have 2-Step Verification
# enabled; GMAIL_APP_PASSWORD is a 16-character App Password from
# https://myaccount.google.com/apppasswords — NOT the account's normal
# login password (Google blocks that for SMTP).
GMAIL_USER=
GMAIL_APP_PASSWORD=
GMAIL_FROM_NAME=Looppr

# Used when EMAIL_PROVIDER=resend. API key + sender for Resend (resend.com).
RESEND_API_KEY=
OTP_FROM_EMAIL=Looppr <onboarding@resend.dev>

# Optional. Real inbox that replies to any automated email should land in —
# used as the Reply-To header by both providers, independent of `from`.
REPLY_TO_EMAIL=

# Used only by `npm run seed:admin` (scripts/seedAdmin.js) — the only way an
# admin account can be created, there is no public admin sign-up. Fill
# these in locally (never commit real values), run the seed script once,
# then remove them / rotate the password.
ADMIN_SEED_NAME=
ADMIN_SEED_EMAIL=
ADMIN_SEED_PHONE=
ADMIN_SEED_PASSWORD=

# Used only by `npm run seed:default-laundromat`
# (scripts/seedDefaultLaundromat.js) — creates/updates the one system-owned
# PartnerUser account that auto-assigned orders fall back to. Safe to
# re-run to rotate the password. Fill these in locally (never commit real
# values).
DEFAULT_LAUNDROMAT_EMAIL=
DEFAULT_LAUNDROMAT_PASSWORD=
DEFAULT_LAUNDROMAT_PHONE=
